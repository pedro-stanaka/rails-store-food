<%= form_for(@allergy_type) do |f| %>
  <% if @allergy_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@allergy_type.errors.count, "error") %> prohibited this allergy_type from being saved:</h2>

      <ul>
      <% @allergy_type.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :description %><br />
    <%= f.text_area :description %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
